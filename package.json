{
  "name": "vibimon",
  "private": true,
  "type": "module",
  "dependencies": {
    "immutable": "^4.3.5"
  },
  "scripts": {
    "build": "bun build src/Client.ts --outfile public/main.js --target browser && rm -rf public/assets && cp -R assets public/assets",
    "server": "bun run build && bun run src/Server.ts"
  }
}
